{
  "name": "birthday-wish-mern-lite",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run dev --workspaces",
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspaces",
    "start": "npm run start --workspace server"
  },
  "workspaces": [
    "server",
    "client"
  ]
}

